cd path\to\mediatmtx.exe

.\mediatmtx.exe

ffmpeg -re -stream_loop -1 -i "resources\parking_720p.mp4" -an -c:v libx264 -preset ultrafast -g 40 -keyint_min 40 -sc_threshold 0 -f rtsp -rtsp_transport tcp rtsp://127.0.0.1:8554/parking

python test.py